# 변경 이력

## 1.9.3

- 작은 그리드 칸과 큰 여백을 조합할 때 발생하던 잘못된 좌표와 크래시 수정. 창 간격은 인접 창 사이 전체 거리로 적용하고 작은 칸에서는 여백을 자동 축소.
- 창 이동·크기 변경의 실제 결과를 확인한 뒤 성공 표시. 권한 부족, 지원하지 않는 창, 앱 응답 지연, 크기 제한 안내.
- 접근성 조회·조작을 전용 작업 큐로 이동하고 타임아웃과 취소 처리 추가.
- 창 크기 지정 클릭의 누름·해제 짝 처리, 단축키 기록 중 전역 스냅 중지, 양방향 단축키 충돌 검증.
- 사용자 지정 크기를 1~16384포인트로 검증하고 잘못된 기존 프리셋 제외.
- 설정 메뉴를 왼쪽 목록으로 전환하고 개인정보 처리방침 링크 추가. 실제 권한 상태만 표시.
- 온보딩 스크롤 및 권한 목록 복구 안내, 자동 실행 실패 안내 유지, 4:3 프리셋 비율 정정.
- Release 회귀 테스트·번들 검증 스크립트 추가.

## 1.9.2

- **자동 업데이트 서명키 교체** — 1.9.1 업데이트가 "improperly signed" 오류로 실패하던 문제. 1.9.1 이하를 쓰고 있다면 이번 한 번만 `brew upgrade --cask oh-my-grid` 또는 DMG로 직접 설치해 주세요. 이후부터는 앱 안에서 자동 업데이트됩니다.

## 1.9.1

- **표시 언어 설정** — 시스템 언어와 무관하게 **영어가 기본**. 설정 → 일반 → Language 에서 English / 한국어를 고르면 재시작 없이 메뉴 막대·설정·시작 가이드가 즉시 바뀜.
- 시작 가이드 첫 화면에 **언어 선택** 단계 추가 — 영어로 시작하고, 한국어 카드를 누르면 가이드부터 바로 한국어로 전환.

## 1.9.0

- **키보드 스냅 단축키** — 마우스 없이 맨 앞 창을 배치: `⌃⌥←/→/↑/↓` 절반, `⌃⌥U/I/J/K` 1/4, `⌃⌥↩` 최대화, `⌃⌥C` 가운데. 실행 시 목표 영역이 잠깐 비춰지고, 설정 → 단축키에서 조합 변경·비활성화 가능(다른 동작·그리드 단축키와 겹치면 거부). 메뉴 막대 → **창 스냅**에서도 실행.
- **영어 지원** — 앱 전체를 영어/한국어로 로컬라이즈(String Catalog). 시스템 언어가 한국어면 한국어, 그 외에는 영어로 표시. README에 영어 소개 추가.
- 여백(가장자리·창 사이) 규칙을 그리드·가장자리·키보드 스냅이 공통으로 사용.
- **설정 창 전면 재설계** — 탭(일반 / 그리드 / 단축키 / 창 크기 / 예외 앱 / 정보)으로 나누고 macOS 표준 그룹 폼 스타일로 교체. 모든 값은 바꾸는 즉시 반영되어 **[적용] 버튼이 사라짐**.
- 그리드 탭에 **라이브 미리보기** — 열·행·여백을 바꾸면 축소 화면에 바로 반영.
- 그동안 UI가 없던 **여백 설정 노출** — 화면 가장자리 여백(0–48px)·창 사이 간격(0–32px) 슬라이더. 그리드 스냅과 가장자리 절반 스냅 모두에 적용.
- **사용자 지정 창 크기 프리셋** — 설정 → 창 크기에서 이름·너비·높이를 편집하고, **창 클릭으로 가져오기**로 아무 창의 현재 크기를 프리셋으로 저장. 메뉴 막대 → 창 크기 고정에 "사용자 지정" 섹션으로 표시.
- 예외 앱 목록에 앱 아이콘·이름 표시, **실행 중인 앱에서 선택**·**앱을 끌어다 놓기**로 추가 가능.
- 정보 탭 추가 — 버전, 업데이트 확인, GitHub·문의·변경 이력 링크.

## 1.8.0

- **첫 실행 시작 가이드** 추가 — ① 손쉬운 사용 권한(허용되면 자동으로 다음 단계) ② 가이드 창 자체를 드래그해 그리드 스냅을 직접 연습(성공 판정 후 창 크기 자동 복귀) ③ 로그인 시 자동 실행·가장자리 스냅·그리드 크기 선택. 메뉴 막대 → **시작 가이드…** 로 언제든 다시 열 수 있음.

## 1.7.0

- 그리드 오버레이 새 디자인 — 등장/퇴장 페이드, 선택 블록 둥근 모서리·글로우, 셀 이동 시 spring 보간, 저대비 그리드 선 + 교차점 도트.
- 선택 블록 중앙에 **셀 수 · 실제 창 크기** 라벨 표시(예: `3 × 2 · 1280 × 720`, 여백 설정 반영). 가장자리 스냅 미리보기에도 `왼쪽 절반 · 1280 × 1415` 식으로 표시.
- 가장자리 절반 스냅 미리보기 때 화면 전체가 어두워지던 디밍 제거 — 하이라이트만 표시.

## 1.6.6

- 권한은 허용돼 있는데 이벤트 탭 설치가 실패하는 경우(업데이트/재서명 직후 TCC 반영 지연) 앱이 재실행 전까지 동작하지 않던 문제 수정 — 성공할 때까지 자동으로 재시도합니다.
- 권한이 없는 상태로 다시 실행하면 아무 안내 없이 조용히 대기하던 문제 수정 — 복구 방법(메뉴의 **권한 다시 요청**)을 짧은 HUD로 안내합니다.
- 권한 대기 폴링(최대 5분)이 끝난 뒤 권한을 허용한 경우, 메뉴를 열거나 설정 창을 확인만 해도 그리드가 즉시 복구됩니다.
- 창 크기 고정 모드 전환 중 권한이 회수되면 입력 탭이 유실된 채 남던 문제 수정 — 권한 감지 경로로 자동 복구합니다.

## 1.6.5

- 손쉬운 사용 권한 **다시 요청** 복구 경로 강화 — 메뉴·설정 버튼이 시스템 prompt를 다시 띄운 뒤 설정 패널을 엽니다(목록에 앱이 안 보일 때 대비).
- 권한이 없는 상태에서 그리드/창 크기 고정을 쓰면 짧은 HUD로 안내합니다.
- 설정 창의 권한 상태가 허용 직후·앱 복귀 시 바로 갱신됩니다.
- 손쉬운 사용 설정 URL을 macOS System Settings 형식으로 열고, 실패 시 이전 형식으로 폴백합니다.
- 권한 대기 폴링은 최대 5분 후 멈추고, 메뉴에서 다시 요청하면 재개합니다.
- 창 크기 고정 호버용 `mouseMoved` 이벤트는 해당 모드일 때만 구독해 평상시 입력 탭 부담을 줄입니다.

## 1.6.4

- 손쉬운 사용 권한 안내가 앱을 실행할 때마다 반복되던 문제 수정 — 시스템 권한 요청은 최초 한 번만 표시하고, 이후에는 메뉴와 설정에서 필요할 때 직접 열 수 있습니다.
- 자동 업데이트 도중 오류가 발생해도 메뉴의 **업데이트 다시 시도…**로 새 업데이트 세션을 시작할 수 있도록 복구 경로를 추가했습니다.

## 1.6.3

- 설정에 **로그인 시 자동 실행** 옵션 추가 — macOS 로그인 항목에 등록해 재시작 후에도 메뉴 막대에서 자동으로 실행됩니다.

## 1.6.2

- 최초 설치 후 손쉬운 사용(접근성) 권한을 켜도 앱을 껐다 켜기 전까지 그리드가 동작하지 않던 문제 수정 — 권한 허용 상태를 감지해 이벤트 탭을 자동으로 다시 시작합니다.

## 1.6.1

- 멀티 모니터 환경에서 그리드가 화면 위쪽 일부를 비워 두고(맨 윗줄 제외) 아래로 밀려 그려지던 문제 수정 — 보조 모니터가 주 모니터보다 아래로 내려간 배치에서 좌표 변환이 어긋나, 그리드 표시·셀 선택·창 스냅 위치가 모두 그만큼 밀리던 버그. 가장자리 절반 스냅·창 크기 고정의 위치 보정에도 동일하게 적용.

## 1.6.0

- 트랙패드 단축키를 **"드래그 중 입력"** 방식으로 변경: 창을 드래그하는 도중 단축키(기본 ⌃⌥G)를 누르면 우클릭과 똑같이 그리드가 켜지고, 셀을 고른 뒤 손을 떼면 배치됨. (1.5.0의 클릭-커밋 단독 모드는 제거)
- 단축키를 **설정에서 변경** 가능: 설정… → 그리드 단축키에서 원하는 조합으로 녹화.
- macOS **시스템·앱 기본 단축키는 지정 불가**: ⌘Space·⌃↑·⇧⌘4·⌘C 등 충돌 위험 단축키를 처음부터 막고, 수정키 없는 키도 거부.

## 1.5.0

- 트랙패드용 그리드 단축키 추가: **⌃⌥G** 를 누르면 커서 아래 창으로 그리드 모드가 켜지고(드래그·우클릭 불필요), 이동해 셀을 고른 뒤 클릭하거나 ⌃⌥G 를 다시 누르면 배치됨(Esc 취소). 기존 "드래그 중 우클릭" 방식은 그대로 유지.

## 1.4.1

- 설정의 그리드 열·행에 **[적용] 버튼** 추가 — 숫자를 고친 뒤 Enter를 누르지 않아도 적용됨(Enter로도 적용 가능).
- 창 크기 고정 안내 HUD의 글자 대비 개선 — 반투명 재질을 불투명 진한 배경으로 바꿔 밝은 배경·화면 녹화에서도 또렷하게 보임.

## 1.4.0

- 창 크기 고정 대상 미리보기: 비율을 고른 뒤 마우스를 움직이면 커서 아래 창이 강조 표시되어, 어떤 창이 바뀔지 클릭 전에 보입니다.

## 1.3.0

- 창 크기 고정 추가: 메뉴바 → **창 크기 고정**에서 비율(4:3 720×480, 16:9 1280×720 / 1920×1080)을 고른 뒤 바꿀 창을 클릭하면 그 크기로 맞춰짐. 좌상단 위치는 유지하고 화면 가시 영역 안으로 보정. (Esc·우클릭으로 취소)

## 1.2.0

- 앱별 예외 목록 추가: 지정한 앱(게임 등)이 맨 앞일 때는 그리드·가장자리 스냅이 마우스 입력에 일절 개입하지 않음 — 드래그·우클릭을 강제하는 게임에서 입력이 가로채이던 문제 해결. 설정 창에서 앱을 추가/제거.

## 1.1.2

- 드래그 감지 때문에 다른 앱 클릭/입력이 지연되던 문제 수정 (창 조회를 첫 드래그 시점에 비동기로 처리, 순수 클릭에는 영향 없음)

## 1.1.1

- 그리드 스냅이 Dock·상단 메뉴바 영역을 침범하던 문제 수정 (사용 영역 기준으로 셀 계산)

## 1.1.0

- 가장자리 절반 스냅 추가: 창을 그냥 끌어서 화면 **좌·우·아래** 끝에 대면 절반, **위** 끝에 대면 최대화
- 텍스트 선택·리사이즈와 구분(창이 실제로 이동할 때만 발동) · 메뉴바/Dock 영역은 침범하지 않음
- 설정에서 "가장자리 절반 스냅" 켜기/끄기 가능 (기본 켜짐)

## 1.0.2

- 새 앱 아이콘 적용 (그리드 + 캐릭터 로고)

## 1.0.1

- 동작 방식 변경: 오른쪽 버튼을 계속 누르지 않고, 드래그 도중 **오른쪽 클릭 한 번**으로 그리드를 켭니다
- 왼쪽 버튼을 놓으면 스냅 완료 · 다시 오른쪽 클릭하거나 Esc로 취소

## 1.0.0

- 첫 릴리스 🎉
- 창을 드래그하는 도중 우클릭하면 화면에 그리드 가이드 표시
- 셀 블록을 끌어 놓으면 창이 해당 영역으로 스냅(이동·리사이즈)
- 그리드 열·행 수 설정(기본 6×4)
- 메뉴바 상주 · Sparkle 자동 업데이트
